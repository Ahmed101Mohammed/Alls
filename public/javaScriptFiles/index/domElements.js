// In this file I determine all elements that I will need in other files.

const productsContainers = ()=> document.querySelectorAll('div.category div.container');// determine all products containers in the home page.
const containerProducts = ()=> document.querySelectorAll('div.category div.container div.product');// determin all products.
const getCategories = ()=> document.querySelectorAll('main div.category');// determin all categories. 
const rightAngles = ()=> document.querySelectorAll('.right-angle');// determin all right angles.
const leftAngles = ()=> document.querySelectorAll('.left-angle');// determin all left angles.